# Worklog

Append-only log. Newest entry on top. Each entry follows
`## YYYY-MM-DD HH:MM — <title>` and runs 2-4 lines describing what was
done, which Claude Code asset was used or created, and what was learned.
The git log shows the *what*; this file shows the *how*.

## 2026-04-28 14:38 — Phase 0 bootstrap

Phase 0 bootstrap: scaffolded repo structure, CLAUDE.md, .claude/ asset
library skeleton, ADR template, eval directories. Generated by Claude
Code via single-prompt bootstrap. Asset library seeded with one
PostToolUse hook, one slash command (`/bootstrap-status`), one subagent
(`adversarial-tester`), and one skill (`adr-writer`). Eval scorecard
runs as a stub with zero cases, ready for Phase 1 dataset.
